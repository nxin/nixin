/* ------------------------------------------------------------------------ *\
   utils.invisible
\* ------------------------------------------------------------------------ */

/**
 * settings
 */
prefix = nix.prefix.root + nix.prefix.util
name = "invisible"
selector = prefix + name


/**
 * [create description]
 * @param  {[type]} selector [description]
 * @param  {[type]} name     [description]
 * @return {[type]}          [description]
 */
nix-visibility--create(selector, name)
