/* ------------------------------------------------------------------------ *\
   utils.reset
\* ------------------------------------------------------------------------ */


/**
 * settings
 */
prefix = nix.prefix.root + nix.prefix.util
name = "reset"
selector = prefix + name


/**
 * styles
 */
[{selector}~="--clear"]
  nix-clear()

[{selector}~="--clearfix"]
  nix-clearFix()

nix-reset()
