/* ------------------------------------------------------------------------ *\
   structure.mixins.util.column
\* ------------------------------------------------------------------------ */


// @TODO change float cols with flex cols or create full grid system like
// util.grid mixin for legacy support

/**
 * [column description]
 * @param  {[type]} cols       [description]
 * @param  {[type]} breakpoint [description]
 * @return {[type]}            [description]
 */
nix-column(cols, breakpoint)
  +above(nix.mq[breakpoint])
    if cols != nix.grid.cols
      float: left

    position: relative
    top: 0
    left: 0
    padding-right: nix.grid.gutter
    padding-left: nix.grid.gutter
    min-height: 1px
    width: (100 / nix.grid.cols) * cols * 1%


/**
 * [last description]
 * @param  {[type]} nth [description]
 * @return {[type]}     [description]
 */
nix-column--last(nth)
  &:nth-child({nth})
    clear: left


/**
 * [equal description]
 * @return {[type]} [description]
 */
nix-column--equal() // @TODO doesn't work with jeet cols
  / core--getParent()
      overflow-y: hidden
  &
    margin-bottom: -9999px
    padding-bottom: 9999px
    height: 100%
