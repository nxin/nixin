/* ------------------------------------------------------------------------ *\
   mixin.mixins.hack.core
\* ------------------------------------------------------------------------ */


ie = nix.legacy.ie
legacy = nix.legacy.supported


hack--ie()
  if (ie == true)
    if(ie == true)
      if (version == legacy)
        {block}
    else
      if (version <= legacy)
        {block}
